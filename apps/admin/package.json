{
  "name": "admin",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "payload dev --port 3001",
    "build": "payload build",
    "start": "payload start --port 3001",
    "generate:types": "payload generate:types",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "payload": "^3.0.0",
    "@payloadcms/db-mongodb": "^3.0.0",
    "@payloadcms/richtext-lexical": "^3.0.0",
    "@payloadcms/plugin-cloud-storage": "^3.0.0",
    "@aws-sdk/client-s3": "^3.700.0",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "typescript": "^5.7.2"
  }
}